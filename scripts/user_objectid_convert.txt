db.emaildatas.find().snapshot().forEach(
    function (doc) {
        db.emaildatas.update(
            {
                _id: doc._id
            },
            {
                $set: {
                    user_id: db.userdetails.findOne({_id:doc.user_id})._id
                }
            }
        );
    }
);


db.authtokens.find().snapshot().forEach(
    function (doc) {
        db.authtokens.update(
            {
                _id: doc._id
            },
            {
                $set: {
                    user_id: db.userdetails.findOne({_id:doc.user_id})._id
                }
            }
        );
    }
);


db.deviceinfos.find().snapshot().forEach(
    function (doc) {
        db.deviceinfos.update(
            {
                _id: doc._id
            },
            {
                $set: {
                    user_id: db.userdetails.findOne({_id:doc.user_id})._id
                }
            }
        );
    }
);